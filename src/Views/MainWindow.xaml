<Window x:Class="FileOrganizerApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="File Organizer" Height="450" Width="800">
    <Grid>
        <Menu VerticalAlignment="Top">
            <MenuItem Header="File">
                <MenuItem Header="Scan Settings" Click="ScanSettings_Click"/>
                <MenuItem Header="Exit" Click="Exit_Click"/>
            </MenuItem>
            <MenuItem Header="Categories" Click="CategoryManager_Click"/>
        </Menu>
        <StackPanel Margin="10">
            <TextBlock FontSize="16" FontWeight="Bold" Text="Organize Your Files" Margin="0,0,0,10"/>
            <Button Content="Start Scan" Width="100" Click="StartScan_Click"/>
            <Button Content="Schedule Scan" Width="100" Click="ScheduleScan_Click" Margin="0,5,0,0"/>
            <ListView Name="FileListView" Margin="0,20,0,0">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="File Name" Width="300" DisplayMemberBinding="{Binding FileName}"/>
                        <GridViewColumn Header="Path" Width="400" DisplayMemberBinding="{Binding FilePath}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>
    </Grid>
</Window>